{
  "name": "crh-kuririn",
  "version": "1.0.0",
  "description": "前端业务跟踪错误上报注入",
  "main": "main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc main.ts && npm run minify",
    "minify": "uglifyjs main.js --compress --mangle -o kuririn.min.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jsmin": "^1.0.1",
    "uglify-js": "^3.9.2",
    "typescript": "^3.8.3"
  }
}
